server:
  port: 8989 #为了以后访问项目不用写端口号

spring:
  datasource:
    url: jdbc:mysql://39.108.67.61:3306/weixin?serverTimezone=GMT%2B8&characterEncoding=utf-8
    username: ROOT
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true